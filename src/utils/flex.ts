import { StyleSheet } from 'react-native';
export const Flex = StyleSheet.create({
  column: {
    display: 'flex',
    flexDirection: 'column',
  },
  row: {
    display: 'flex',
    flexDirection: 'row'
  }
});
